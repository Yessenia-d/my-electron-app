name: Build and Publish Electron App

on:
  push:
    branches:
      - main
      - dev  # 如果你希望在 dev 分支也触发发布

jobs:
  build-linux:
    runs-on: ubuntu-latest  # 在 Ubuntu 上构建 Linux 版本
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22'  # 使用适合你的项目的 Node.js 版本

      - name: Install dependencies
        run: pnpm install

      - name: Build and package Electron app for Linux
        run: pnpm make -- --arch x64  # 构建 Linux x64 版本的应用

      - name: Publish to GitHub Releases
        run: pnpm publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-mac:
    runs-on: macos-latest  # 在 macOS 上构建 macOS 版本
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22'

      - name: Install dependencies
        run: pnpm install

      - name: Build and package Electron app for macOS
        run: pnpm make -- --arch x64  # 构建 macOS x64 版本的应用

      - name: Publish to GitHub Releases
        run: pnpm publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest  # 在 Windows 上构建 Windows 版本
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22'

      - name: Install dependencies
        run: pnpm install

      - name: Build and package Electron app for Windows
        run: pnpm make -- --arch x64  # 构建 Windows x64 版本的应用

      - name: Publish to GitHub Releases
        run: pnpm publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
